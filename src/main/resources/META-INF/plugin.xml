<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.npmpeerdependencycompanion</id>
    <name>NPM Peer Dependency Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning icon on a <code>package.json</code>
        <code>peerDependencies</code> entry whose real installed version
        (from <code>node_modules</code>) doesn't satisfy the declared
        semver range -- or isn't installed at all. npm's own peer
        dependency model is opt-in and silent: since npm 7 it auto-installs
        a compatible peer if it can, but a version mismatch, an unresolved
        conflict, or a peer installed via a workspace/hoisting quirk all
        produce no warning in the editor, only a runtime failure the first
        time the mismatched API is actually called.</p>
        <p><b>100% static JSON PSI analysis</b> -- reads
        <code>peerDependencies</code> and the installed package's own
        <code>package.json</code> <code>"version"</code> field using the
        bundled JSON plugin's real PSI, no npm CLI invocation, no
        network calls.</p>
        <p>Stated honestly: v0.1's semver-range checker covers
        <code>^</code>/<code>~</code>/comparison/exact/<code>*</code>
        ranges -- an OR range (<code>||</code>) or a hyphen range is
        skipped, never reported as a false failure. A monorepo/workspace
        layout where <code>node_modules</code> lives at a different level
        than this <code>package.json</code> isn't resolved (no hoisting
        simulation), a possible false positive in that specific setup.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.json</depends>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JSON"
            implementationClass="dev.gaphunter.npmpeerdependencycompanion.gutter.UnsatisfiedPeerDependencyLineMarkerProvider"/>
        <notificationGroup id="NPM Peer Dependency Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
